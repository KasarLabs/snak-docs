This explains how to signature mode work

The signature mode works the following way

the client receives a prompt -> it askes the agent to select the right schema -> all tools have the ability to either execute or return call data waiting for client signature -> if signature is configured the multicall transaction is sent to front end